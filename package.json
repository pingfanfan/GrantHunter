{
  "name": "uk-academic-funding-hub",
  "version": "1.0.0",
  "description": "Daily-updated UK academic funding aggregator for GitHub Pages",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "update:data": "node scripts/update-funding.mjs",
    "send:digest": "node scripts/send-digest.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run test && npm run validate:data",
    "refresh": "npm run update:data && npm run send:digest"
  }
}
